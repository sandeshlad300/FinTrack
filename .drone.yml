kind: pipeline
type: docker
name: android-ci

trigger:
  branch:
    - main
    - master

steps:
  - name: build-debug
    image: ghcr.io/cirruslabs/android-sdk
    commands:
      - chmod +x gradlew
      - ./gradlew clean assembleDebug
