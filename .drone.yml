kind: pipeline
type: docker
name: android-ci

trigger:
  branch:
    - main
    - master

steps:
  - name: build-debug
    image: ghcr.io/cirruslabs/android-sdk:34
    commands:
      - chmod +x gradlew
      - ./gradlew clean assembleDebug
